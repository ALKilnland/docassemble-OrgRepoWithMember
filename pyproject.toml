[build-system]
requires = [
    "setuptools>=80.9.0",
]
build-backend = "setuptools.build_meta"

[project]
name = "docassemble.OrgRepoWithMember"
version = "0.0.1"
description = "ALKilnSetup tests for an org repo docassemble package"
readme = "README.md"
authors = [
    { name = "", email = "66677730+alkilnert@users.noreply.github.com" },
]
dependencies = []
license = "MIT"
license-files = [
    "LICENSE",
]

[project.urls]
Homepage = "https://docassemble.org"

[tool.setuptools.packages.find]
where = [
    ".",
]
